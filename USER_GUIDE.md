# User Guide

1. Start the platform using `make dev`.
2. Generate deterministic datasets using `make seed`.
3. Open `http://localhost:3000` to access the FaultTrace Dashboard.
4. Navigate to **Run Lab** to execute pipeline combinations.
